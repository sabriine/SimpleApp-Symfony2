{% extends "FormalandCatalogueBundle::layout.html.twig" %}
{% block title %} FormalandCatalogueBundle:indexArticle {% endblock %}
{% block body %}
    
 <div class="box-content"><label style="margin-left:350px;" class="control-label" for="idSuccess">
       {%for message in app.session.flashbag.get('info') %}
           {{message}}
           {%endfor%}
       
       
   </label>
						<table class="table table-striped table-bordered bootstrap-datatable datatable">
						  <thead>
							  <tr>
								  <th>Nom</th>
								  <th>Description</th>
								  <th>Prix</th>
								  <th>Quantite</th>
                                                                  <th>Categorie</th>
                                                                   <th>Image</th>
                                                                   <th>Actions</th>
							  </tr>
						  </thead>  
                                                  {% for article in cat %}
						  <tbody>
							<tr>
								<td>{{article.nom}}</td>
								<td class="center"> {{article.description}}</td>
                                                                <td class="center"> {{article.prix}}</td>
                                                                <td class="center"> {{article.quantite}}</td>
								<td class="center">{{article.categories.nom}}
									
                                                                                                                                               
	</td>
        <td class="center"><img src="{{ asset(article.Image.getWebPath())}}" width="100" height="100"/> </td>
								<td class="center">
									
									<a class="btn btn-success" href="{{path('edit_article',{'token':article.token})}}">
										Modifier<i class="halflings-icon white zoom-in"></i>  

									</a>
									<a class="btn btn-danger" href="{{path('delete_article',{'token':article.token})}}">
										Supprimer<i class="halflings-icon white edit"></i>  
									</a>
									
								</td>
							</tr>
                                                        {% endfor %}

                                                         <table><td>          <a class="btn btn-info" href="{{path('add_article')}}">
										Ajouter article<i class="halflings-icon white trash"></i> 
									</a>
									</td>
									<td>
                                                        
									 <a class="btn btn-info" href="{{path('index_catalogue')}}">
										Catégories <i class="halflings-icon white trash"></i>  
									</a></td></table>
							   </br></br>
   {% endblock %}    

