{% extends "FormalandCatalogueBundle::layout.html.twig" %}
{% block title %} FormalandCatalogueBundle:index {% endblock %}
{% block body %}
   
   <div class="box-content"><label style="margin-left:350px;" class="control-label" for="idSuccess">
       {%for message in app.session.flashbag.get('info') %}
           {{message}}
           {%endfor%}
       
       
   </label>
						<table class="table table-striped table-bordered bootstrap-datatable datatable">
						  <thead>
							  <tr>
								  <th>Nom</th>
								  <th>Description</th>
								  <th>Status</th>
								  <th>Actions</th>
							  </tr>
						  </thead>  
                                                  {% for categorie in cat %}
						  <tbody>
							<tr>
								<td>{{categorie.nom}}</td>
								<td class="center"> {{categorie.description}}</td>
								<td class="center">
									
                                                                            {% if categorie.status==1 %}
                                                                               <span class="label label-success">
                                                                            Active
                                                                            </span>
                                                                            {% else %}
                                                                                  <span class="label label-warning">
                                                                            Non Active
                                                                                  </span>                                                                             </span>

                                                                            {% endif %}
                                                                            
                                                                           
								</td>
								<td class="center">
									<a class="btn btn-success" href="{{path('edit_catalogue',{'token':categorie.token})}}">
										Modifier    <i class="halflings-icon white zoom-in"></i>  
									</a>
									<a class="btn btn-danger" href="{{path('delete_catalogue',{'token':categorie.token})}}">
										Supprimer<i class="halflings-icon white edit"></i>  
									</a>
									
								</td>
							</tr>
                                                        {% endfor %}

                                             <table><td>          <a class="btn btn-info" href="{{path('add_catalogue')}}">
										Ajouter Catégorie<i class="halflings-icon white trash"></i> 
									</a>
									</td>
									<td>
                                                        
									 <a class="btn btn-info" href="{{path('index_article')}}">
										Articles <i class="halflings-icon white trash"></i>  
									</a></td></table>
							   </br></br>
   
{% endblock %}    